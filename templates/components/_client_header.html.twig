<header class="top-0 left-0 right-0 z-50 fixed">
	<nav class="bg-slate-800 text-white py-4 h-16">
		<div class="container mx-auto flex items-center justify-between">
			<a href="{{ path("app_client_dashboard") }}" class="text-lg font-bold">Mentor APP | {% if is_granted("ROLE_STUDENT") %} Student {% else %} Mentor {% endif %} </a>
            <span class="text-lg font-bold">{{ app.user.lastname ~" " ~ app.user.firstname }}</span>
			<a href="{{ path("app_logout") }}" class="text-gray-300 font-medium hover:text-gray-50">Deconnexion</a>
		</div>
	</nav>
</header>
